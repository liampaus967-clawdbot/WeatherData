# Driftwise Weather Data Pipeline - Python Dependencies
# Install with: pip install -r requirements.txt
# Note: Requires system GDAL and eccodes libraries (see setup_venv.sh)

# Weather data access
herbie-data>=2024.0.0

# Geospatial processing
rioxarray>=0.15.0
xarray>=2023.0.0
cfgrib>=0.9.10
GDAL  # Binds to system GDAL

# AWS integration
boto3>=1.28.0

# Configuration
pyyaml>=6.0

# Data processing utilities
pandas>=2.0.0
numpy>=1.24.0,<2.0.0  # Pin to NumPy 1.x for GDAL compatibility
scipy>=1.11.0  # For KDTree reprojection in wind tiles

# HTTP utilities (for Herbie)
requests>=2.31.0

# Additional dependencies for xarray/rioxarray
netCDF4>=1.6.0
h5netcdf>=1.2.0
dask>=2023.0.0
