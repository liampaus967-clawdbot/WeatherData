# Driftwise Weather Data Pipeline Configuration
# Copy this file to .env and configure for your environment

# =============================================================================
# AWS Configuration
# =============================================================================
# S3 bucket for weather data outputs (COGs, tiles, metadata)
S3_BUCKET=your-bucket-name

# AWS region for S3 operations
AWS_REGION=us-east-1

# AWS profile name (from ~/.aws/credentials)
# Leave empty to use default credentials chain
AWS_PROFILE=

# =============================================================================
# Upload Settings
# =============================================================================
# Enable/disable S3 uploads (set to false for local-only processing)
ENABLE_S3_UPLOAD=true

# =============================================================================
# Processing Options
# =============================================================================
# Enable tile generation (PNG tiles for web maps)
ENABLE_TILES=true

# Zoom levels for tile generation (e.g., "0-6", "0-8")
ZOOM_LEVELS=0-6

# Number of parallel processes for tile generation
TILE_PROCESSES=4

# Forecast hours to download (e.g., "0-12", "0-6", "0,3,6,9,12")
FORECAST_HOURS=0-12

# =============================================================================
# Directories
# =============================================================================
# Working directory for temporary files (downloads, processing)
WORK_DIR=/tmp/weather-pipeline

# Log directory
LOG_DIR=/var/log/weather-pipeline

# =============================================================================
# Advanced Options (optional)
# =============================================================================
# Processing priority (1-4, affects which variables are extracted)
# PRIORITY=1

# Custom model cycle (00, 06, 12, 18) - defaults to current hour
# MODEL_CYCLE=

# Custom model date (YYYY-MM-DD) - defaults to today
# MODEL_DATE=
